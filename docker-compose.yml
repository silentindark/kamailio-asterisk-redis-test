version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    networks:
      - voip-net

  kamailio:
    build:
      context: ./kamailio
      dockerfile: Dockerfile    
    container_name: kamailio
    network_mode: host
    volumes:
      - ./kamailio/kamailio.cfg:/etc/kamailio/kamailio.cfg:ro
      - ./kamailio/dispatcher.list:/etc/kamailio/dispatcher.list            
      - ./kamailio/update_states.py:/etc/kamailio/update_states.py:ro            
      - ./kamailio/init.sh:/etc/kamailio/init.sh:ro
      - ./kamailio/aliases.cfg:/etc/kamailio/aliases.cfg:ro
    depends_on:
      - redis      
    restart: unless-stopped    

  asterisk1:
    build:
      context: ./asterisk
      dockerfile: Dockerfile        
    container_name: asterisk1    
    network_mode: host
    volumes:
      - ./asterisk/asterisk.conf:/etc/asterisk/asterisk.conf
      - ./asterisk/extensions.conf:/etc/asterisk/extensions.conf
      - ./asterisk/modules.conf:/etc/asterisk/modules.conf
      - ./asterisk/pjsip.conf.asterisk1:/etc/asterisk/pjsip.conf
      - ./asterisk/manager.conf.asterisk1:/etc/asterisk/manager.conf
      - ./asterisk/musiconhold.conf:/etc/asterisk/musiconhold.conf      
      - ./asterisk/rtp.conf.asterisk1:/etc/asterisk/rtp.conf            
      - ./asterisk/queues.conf:/etc/asterisk/queues.conf        
    restart: unless-stopped

  asterisk2:
    build:
      context: ./asterisk
      dockerfile: Dockerfile        
    container_name: asterisk2    
    network_mode: host   
    volumes:
      - ./asterisk/asterisk.conf:/etc/asterisk/asterisk.conf
      - ./asterisk/extensions.conf:/etc/asterisk/extensions.conf
      - ./asterisk/modules.conf:/etc/asterisk/modules.conf
      - ./asterisk/pjsip.conf.asterisk2:/etc/asterisk/pjsip.conf
      - ./asterisk/manager.conf.asterisk2:/etc/asterisk/manager.conf
      - ./asterisk/musiconhold.conf:/etc/asterisk/musiconhold.conf
      - ./asterisk/rtp.conf.asterisk2:/etc/asterisk/rtp.conf  
      - ./asterisk/queues.conf:/etc/asterisk/queues.conf                              
    restart: unless-stopped

  listener1:
    build:
      context: ./listener
      dockerfile: Dockerfile
    container_name: listener1
    networks:
      - voip-net
    volumes:
      - ./listener/listener.py:/app/listener.py
    environment:
      - ASTERISK_SERVER=192.168.100.127
      - POD_NAME=asterisk1
      - ASTERISK_PORT=5038
      - PYTHONUNBUFFERED=1      
    depends_on:
      - redis
      - asterisk1
      - asterisk2      
    restart: unless-stopped

  listener2:
    build:
      context: ./listener
      dockerfile: Dockerfile
    container_name: listener2
    networks:
      - voip-net
    volumes:
      - ./listener/listener.py:/app/listener.py
    environment:
      - ASTERISK_SERVER=192.168.100.127
      - POD_NAME=asterisk2
      - ASTERISK_PORT=5039
      - PYTHONUNBUFFERED=1
    depends_on:
      - redis
      - asterisk1
      - asterisk2      
    restart: unless-stopped

networks:
  voip-net:
    driver: bridge